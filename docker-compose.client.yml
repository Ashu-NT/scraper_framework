services:
  scraper:
    image: ${SCRAPER_IMAGE:-ghcr.io/your-org/scraper-framework-client:1.0.0}
    working_dir: /app
    volumes:
      - ./configs:/app/configs
      - ./output:/app/output
      - ./secrets:/app/secrets:ro
    command: ["configs/jobs/client_template.yaml"]
